import 'package:baitaplon/constants/myColors.dart';
import 'package:baitaplon/models/Users.dart';
import 'package:baitaplon/page/auth/LoginPage.dart';
import 'package:baitaplon/page/auth/SignupPage.dart';
import 'package:baitaplon/page/bottom/HomePage.dart';
import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:http/http.dart' as http;

void main() {
  testWidgets('Signup ...', (tester) async {
    // final name = find.byKey(ValueKey('nameSignup'));
    // final username = find.byKey(ValueKey('usernameSignup'));
    // final password = find.byKey(ValueKey('passwordSignup'));
    // final signupButton = find.byKey(ValueKey('signup'));

    // await tester.pumpWidget(SignupPage());
    // await tester.enterText(name, "");
    // await tester.enterText(username, "");
    // await tester.enterText(password, "");
    // await tester.tap(signupButton);

    // expect(SignupPage(), HomePage());
  });
}
